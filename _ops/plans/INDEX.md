# Plans — index

One row per plan doc. Status: `active` · `draft` · `done` (move the file to
`para/archive/` when done; keep the row here as the ledger).

| Plan | Status | Note |
|------|--------|------|
