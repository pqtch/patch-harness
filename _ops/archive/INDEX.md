# archive — the record of having been wrong

Dead plans (`plans/`), retired docs (`docs/`), closed sketches (`sketch/`). Moving a file here
IS finishing it. Nothing in here is a live claim; the pointer check skips it.

## Index
*(empty)*
