# inbox — lessons with no home yet

A `lesson` note (`~/workspace/.claude/templates/NOTE.tmp.md`) lands here only until it has a
home. Staging, never a destination: if a file has sat here across several sessions, either
file it or cut it.

## Index
*(empty)*
