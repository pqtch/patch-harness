# Shared memory — routing index

One line per durable fact, pointing to its detail file in `memories/shared/memory/`.
Do not write fact content here — this file is an index, not storage.

- *(empty — populated during `/setup` and ongoing sessions)*

<!-- format: - [Short title](memory/<slug>.md) — one-line hook -->
