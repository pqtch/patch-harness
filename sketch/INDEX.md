# sketch/ — pre-projects

An unofficial project not yet committed to; a new sketch directory is created whenever a
session opens a new topic. **Deleting a sketch must cost nothing** — if deleting it would
hurt, it was a project, and it moves to `~/workspace/projects/`.

## Index
| Path | Description |
|---|---|
| *(none yet)* | |

One-line ideas that are not yet a sketch go under `## Ideas` in `~/workspace/.claude/STICKY.md`.
A finished investigation does not stay here reading as live: it moves to
`~/workspace/_ops/archive/sketch/`. Location is the status. <!-- may-be-absent -->
