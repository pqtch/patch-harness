# craft/center

This specialist's craft — skills that belong to it across every project, loaded by walking in here.

## Routing
| Path | What lives there |
|---|---|
| `~/workspace/craft/center/.claude/skills/` | this specialist's private skill pool <!-- may-be-absent --> |

The pool is empty; nothing failed to load. That sentence is the mechanism: an empty pool and
a pool that silently failed to attach look identical from inside a session, so the index
says which it is.
