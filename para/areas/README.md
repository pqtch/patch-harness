# Areas

Ongoing responsibilities — things with no end date, unlike `para/projects/`. Give each area
a short README or note file if it needs one; keep it as thin as a project's CONTEXT.md.

`ideas/` is where loose thoughts land before they're a project — see its README for
the capture tiers.
