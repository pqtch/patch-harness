# Archive

Closed work lands here — finished projects, retired ideas, the old `SETUP.md` after
first run. Archive is the default move; delete is never the default (see
`.claude/rules/conventions.md`).

Keep the original folder/file structure when you move something in; don't reorganize
on the way. If you need it back, move it out — nothing here is meant to be permanent.
